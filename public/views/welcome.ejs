<html>
<head>
  <script>
    function openAndCloseTab(url) {
      
      window.open(url);

      setTimeout(function() {
        
     
      }, 5000);
    }
    
    let val = 7000
  // openAndCloseTab(`http://localhost:${val}/cl`);
        </script>
  <title> Login - Highnote</title>
  </head>
  
  <body>
    <span id='variableJSON' hidden>
      <%= JSON.stringify(a); %>
  </span>
        <div class="row mt-5">
          <div class="col-md-6 m-auto">
            <div class="card card-body text-center">
             <%= a %>
              <h1><i class="fab fa-node-js fa-3x"></i></h1>
              <p>Create <b>HIGH SCORE </b> account or login</p>
              <a href="/register" class="btn btn-primary btn-block mb-2"
                > Register</a>
              <a href="/login" class="btn btn-secondary btn-block">Login</a>
            </div>

          </div>
        </div>
        </body>
        </html>